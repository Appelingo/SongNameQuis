# IntroQ セキュリティ FAQ

> 最終更新: 2026-09-13 / 公開先: https://song-name-quis.vercel.app/
> 関連: [PLAN.md](PLAN.md)（判断 9 など設計の根拠）, [DEPLOY.md](DEPLOY.md)

このアプリは**サーバーを持たない静的サイト**です。処理はすべてブラウザ内で動き、
データは Supabase に、音楽は Apple の API に直接問い合わせます。
その構造ゆえに「隠せないもの」と「守れるもの」がはっきり分かれます。以下はその整理です。

---

## Q1. アプリのソースに API キーが埋まっていると聞いたが、大丈夫か

**埋まっています。そして隠せません。** これは設計上の前提です。

`EXPO_PUBLIC_` で始まる環境変数は、ビルド時に JavaScript に焼き込まれ、
ブラウザに配信されます。誰でも開発者ツールから取り出せます。対象は 3 つです。

| 値 | 取り出されると何ができるか |
|---|---|
| Supabase の URL | 単体では何もできない |
| Supabase の anon key | **公開前提の鍵。** 保護は鍵ではなく RLS（Q2）が担う |
| Apple Music の developer token | Apple の**カタログ情報**の取得のみ（Q4） |

Vercel で環境変数を `secret` にしようとすると拒否されますが、これは正しい挙動です。
`secret` にしてもバンドルへの埋め込みは防げないため、
「秘匿されている」という誤解を生まないよう Vercel が止めています。

**本当に秘匿すべきなのは `scripts/AuthKey_*.p8`（Apple の秘密鍵）だけ**で、
これは `.gitignore` で除外され、Vercel にも置いていません。

---

## Q2. anon key が公開なら、誰でもデータベースを読み書きできるのでは

**できません。RLS（Row Level Security）で防いでいます。**

鍵は「どのプロジェクトか」を示すだけで、「何をしてよいか」は行単位のポリシーが決めます。
各ユーザーは匿名サインインで固有の ID を持ち、その ID に紐づく行しか触れません。

**2026-09-13 に本番環境へ実際にリクエストを投げて検証した結果:**

| 試したこと | 結果 |
|---|---|
| 未認証（anon key だけ）で全テーブルを読む | **すべて空。** 何も読めない |
| 匿名サインイン後、他人のライブラリを読む | **0 件** |
| 匿名サインイン後、他人の参加者情報を読む | **0 件** |
| 匿名サインイン後、他人のルームを書き換える | **更新 0 行**（拒否） |

主張ではなく実測です。

---

## Q3. 自分の音楽ライブラリはどう扱われるか

**これが最も気を遣っている点です。** 曲の好みは、思想や属性を推測させ得る情報だからです。

- **保存されるもの**: 曲名・アーティスト名・カタログ ID
- **保存されないもの**: 再生履歴、プレイリスト、購入履歴、Apple ID、メールアドレス
- **見える範囲**: **同じルームの参加者だけ**。無関係な人からは 1 件も読めません（Q2 で実測）
- **保持期間**: **出題リストが作られた時点で削除されます。** ロビーにいる数分間だけです

「曲を追加せずに参加」を選べば、ライブラリを一切送らずに遊べます。

> **既知の制約**: ルーム情報（ルームコード、出題される 20 曲）は、
> 匿名サインインした人なら読めます。ルームコードで参加できるようにするため、
> ルームの検索を開けてあるためです。
> 個人のライブラリとは切り離されており、実害は小さいと判断しています。

---

## Q4. Apple Music のアカウントは安全か

**アプリはあなたの Apple Music アカウントに触れません。**

Apple Music のライブラリを読むには **Music User Token** が必要です。これは
あなたがサインインしたときに Apple が発行し、**あなたのブラウザ内にのみ保存されます**。
アプリのサーバーにも Supabase にも送っていません（コード上に保存処理が存在しません）。

公開されている developer token では、あなたのライブラリは読めません。
**2026-09-13 に実際に試した結果:**

```
developer token だけでカタログを読む    → HTTP 200（曲名などの公開情報）
developer token だけでライブラリを読む  → HTTP 403 Forbidden
```

developer token が悪用された場合の影響は、
「開発者のカタログ API 利用枠が消費される」ことに限られます。
利用者の個人データには到達しません。

---

## Q5. どんな個人情報を集めているか

**集めていません。** 保存されるのは以下だけです。

| 項目 | 内容 |
|---|---|
| ニックネーム | あなたが入力した文字列（本名を入れない限り本名は入りません） |
| 匿名ユーザー ID | ランダムな UUID。メールアドレスや Apple ID とは結び付いていません |
| ライブラリの曲名 | Q3 のとおり、出題リスト作成後に削除 |

アカウント登録もメールアドレスの入力もありません。アクセス解析も入れていません。

---

## Q6. 知らない人にゲームを壊されないか

**壊せません。** ゲームの進行（曲を進める、終了する）はホストしか実行できません。
Q2 の実測で、他人のルームへの書き換えが 0 行で拒否されることを確認しています。

ルームコードを知られると**参加**はされ得ます。身内で遊ぶ前提であれば、
コードを不用意に共有しないことで十分です。

---

## Q7. 残っているリスクは

正直に挙げます。

| リスク | 影響度 | 備考 |
|---|---|---|
| ルーム一覧と出題曲は認証すれば読める | 低 | 塞ぐには参加処理の作り替えが必要。個人ライブラリは対象外 |
| developer token の悪用 | 低 | カタログ API 枠の消費のみ。2027-02-23 に自動失効 |
| 匿名サインインの乱用 | 低 | 悪意ある連打で無料枠（月 5 万）を消費され得る。captcha 未導入 |
| ブラウザのデータ消去 | — | 自分が作ったルームの操作権を失います（ゲームは使い捨てなので許容） |

---

## Q8. プレビュー音源を使うことは Apple の規約上問題ないか

**断定できません。安全側に倒した設計にしています。**

Apple のガイダンスは MusicKit を
*"intended for simple music playback by Apple Music subscribers"*（Apple Music 契約者による
単純な音楽再生を想定）と記述しており、プレビュー音源のみを使う統合は
**グレーゾーン**とされています。Apple Developer Forums でも同種の質問に明確な回答が見当たりません。

そのため、**曲を再生するホストには Apple Music への接続を必須**にしています。
「契約者が自分の権限で音楽を鳴らし、同席者がそれを聴く」という、
車内でスピーカーから流すのと同じ構図に揃えるためです。
同席して聴くだけの参加者には、契約も認証も求めません。

確実な答えが必要な場合は、Apple Developer Relations への照会が必要です。

---

## Q9. 問題を見つけたら

リポジトリの Issue か、開発者へ直接連絡してください。
身内向けの遊びとして作られたアプリであり、
金銭や認証情報を扱わない前提で設計されています。
